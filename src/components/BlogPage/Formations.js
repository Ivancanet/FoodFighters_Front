import React from 'react';
import { Link } from 'react-router-dom';
import BlogService from './BlogService'
import ReactHtmlParser from 'react-html-parser'

const Formations = () =>{
    return(
        <div>
        <h1>Formations Under Construction</h1>
        </div>
    );
};

export default Formations;